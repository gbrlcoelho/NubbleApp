import React from 'react';
import {View} from 'react-native';

export const Camera = () => {
  return <View />;
};

export const useCameraDevice = jest.fn();
export const useCamearaFormat = jest.fn();
